package com.hidex.app

import android.app.Application

/**
 * Application class for hideX.
 * Used for initialization of global components.
 */
class HideXApplication : Application() {
    
    override fun onCreate() {
        super.onCreate()
        // Initialize any global components here if needed
    }
}
